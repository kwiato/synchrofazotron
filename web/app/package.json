{
  "name": "synchrofazotron-panel",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Preact control panel for the Synchrofazotron audio player. Built on the laptop; the Pi only serves the static bundle.",
  "scripts": {
    "dev": "vite",
    "build:css": "sass ../ui/style.scss ../ui/style.css --no-source-map --style=expanded",
    "build": "npm run build:css && vite build",
    "build:cap": "npm run build:css && vite build --mode capacitor",
    "cap:sync": "npm run build:cap && cap sync android",
    "preview": "vite preview"
  },
  "dependencies": {
    "@capacitor/android": "^7.6.7",
    "@capacitor/app": "^7.1.2",
    "@capacitor/browser": "^7.0.5",
    "@capacitor/core": "^7.6.7",
    "capacitor-zeroconf": "^4.0.0",
    "preact": "^10.24.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.6.7",
    "@preact/preset-vite": "^2.9.3",
    "sass": "^1.101.0",
    "vite": "^6.0.7"
  }
}
